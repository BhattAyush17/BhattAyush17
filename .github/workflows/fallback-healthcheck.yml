name: Fallback Health Check

on:
  schedule:
    - cron: "30 */6 * * *"
  workflow_dispatch:

jobs:
  verify-assets:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verify generated assets
        run: |
          test -f assets/stats/github-stats.svg
          test -f assets/stats/languages.svg
          test -f assets/stats/streak.svg
          test -f assets/stats/contribution-graph.svg
          test -f assets/stats/summary-stats.svg
          test -f assets/stats/summary-repos-per-language.svg
          test -f assets/stats/summary-most-commit-language.svg
          test -f assets/stats/summary-productive-time.svg
          test -f assets/snake/github-contribution-grid-snake-dark.svg

